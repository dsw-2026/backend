# Dependencias
node_modules/

# Compilado de TypeScript
dist/
*.tsbuildinfo

# Variables de entorno (credenciales, nunca se suben)
.env
.env.*
!.env.example

# Logs
*.log

# Archivos del sistema operativo (Mac)
.DS_Store

# Cache de npm/pnpm
.npm

# Editor: se ignora todo .vscode salvo lo que sí queremos compartir con el equipo
.vscode/*
!.vscode/launch.json
# Archivos subidos por los usuarios (fotos) — no se versionan
uploads/
